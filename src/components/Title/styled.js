import styled from 'styled-components';

export const StyleTitle = styled.title`
    display: flex;
    flex-direction: column;
    align-items: left;
    width: 100%;
    font-size: 40px;
    font-weight: bold;
    color: #000;
`