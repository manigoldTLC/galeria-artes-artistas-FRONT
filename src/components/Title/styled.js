import styled from 'styled-components';

export const StyleTitle = styled.title`
    display: flex;
    flex-direction: column;
    align-items: left;
    width: 100%;
    font-size: 2.2rem;
    font-weight: bold;
    color: #000;

    span {
        margin-top: 10px;
        font-size: 1.4rem;
        font-weight: normal;
    }
`